---
phase: 27-foundation-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert-avatars-to-webp.ts
  - public/images/alex-avatar.webp
  - public/images/kim-avatar.webp
  - public/images/collaborative-avatar.webp
  - app/(admin)/admin/landing-page/page.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Subscribe page loads avatar images in WebP format instead of PNG"
    - "Developer has documented baseline metrics (bundle sizes, LCP times) for comparison"
  artifacts:
    - path: "scripts/convert-avatars-to-webp.ts"
      provides: "Sharp-based image conversion script"
      contains: "sharp"
      min_lines: 30
    - path: "public/images/alex-avatar.webp"
      provides: "WebP version of Alexandria avatar"
    - path: "public/images/kim-avatar.webp"
      provides: "WebP version of Kim avatar"
    - path: "public/images/collaborative-avatar.webp"
      provides: "WebP version of Collaborative avatar"
    - path: "app/(admin)/admin/landing-page/page.tsx"
      provides: "Image components using WebP with priority prop"
      contains: "alex-avatar.webp"
    - path: ".planning/phases/27-foundation-quick-wins/27-BASELINE.md"
      provides: "Performance and bundle size baseline metrics"
      min_lines: 15
  key_links:
    - from: "scripts/convert-avatars-to-webp.ts"
      to: "sharp"
      via: "import and conversion calls"
      pattern: "sharp.*webp"
    - from: "app/(admin)/admin/landing-page/page.tsx"
      to: "/images/*.webp"
      via: "Image src prop"
      pattern: 'src="/images/.*\\.webp"'
---

<objective>
Optimize avatar images for faster page loads and establish performance baseline for Phase 31 validation.

Purpose: Reduce image file sizes by 50-70% through WebP format conversion, improve LCP for pages using avatars, and document current performance metrics before applying further optimizations.

Output: WebP avatar images, updated components using optimized images with priority hints, and baseline metrics document.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-foundation-quick-wins/27-RESEARCH.md
@app/(admin)/admin/landing-page/page.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WebP conversion script and convert avatars</name>
  <files>
    scripts/convert-avatars-to-webp.ts
    public/images/alex-avatar.webp
    public/images/kim-avatar.webp
    public/images/collaborative-avatar.webp
    package.json
  </files>
  <action>
Install sharp as devDependency for script usage (sharp is already a transitive Next.js dependency, but explicit install needed for standalone scripts per research):
```bash
pnpm add -D sharp
```

Create scripts/convert-avatars-to-webp.ts following the pattern from research:
- Target exactly 3 files: alex-avatar.png, kim-avatar.png, collaborative-avatar.png
- Check metadata.hasAlpha to preserve transparency
- Use quality 85 with alphaQuality 100 for transparency preservation
- Log input/output file sizes and reduction percentage
- Handle missing files gracefully with try/catch

Run the conversion script:
```bash
npx tsx scripts/convert-avatars-to-webp.ts
```

Verify all 3 WebP files created in public/images/ directory. KEEP original PNG files (don't delete) — per research recommendation, keep for 1-2 sprints as rollback option.

Expected output: 50-70% file size reduction with transparency preserved.

DO NOT convert favicon or logo PNGs — only the three avatar files specified in PERF-01.
  </action>
  <verify>
Check WebP files exist:
```bash
ls -lh public/images/*.webp
```

Verify transparency preserved and quality acceptable (visually inspect one WebP in browser).

Confirm conversion script output shows file size reduction percentage.
  </verify>
  <done>
sharp installed as devDependency. scripts/convert-avatars-to-webp.ts exists. Three WebP files created in public/images/. Script output shows 50-70% size reduction. Original PNGs still exist. Transparency preserved in WebP files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Image components to use WebP with priority prop</name>
  <files>
    app/(admin)/admin/landing-page/page.tsx
  </files>
  <action>
Update app/(admin)/admin/landing-page/page.tsx to use WebP versions:

Find Image components at lines ~509 and ~590 (Alexandria and Kim avatars per grep results). Change src paths:
- "/images/alex-avatar.png" → "/images/alex-avatar.webp"
- "/images/kim-avatar.png" → "/images/kim-avatar.webp"

Add priority prop to these Image components (PERF-02). Project uses Next.js 15.5.11, so use priority={true} (not preload which is Next.js 16+).

IMPORTANT: Based on research anti-pattern guidance, only add priority to ONE image per page maximum — the true LCP candidate. Since admin landing page may have multiple sections, assess which avatar is actually above-fold. If both are below-fold, use loading="eager" instead of priority.

If collaborative-avatar.png is also used in this file (grep didn't show it, but check), update to WebP as well.

DO NOT add priority to multiple images — this degrades LCP by 400-1200ms per research Pitfall 2.
  </action>
  <verify>
Search for remaining .png references to avatars:
```bash
grep -r "alex-avatar.png\|kim-avatar.png\|collaborative-avatar.png" app/ --include="*.tsx"
```

Should return zero results (all updated to .webp).

TypeScript compilation succeeds:
```bash
npx tsc --noEmit
```

Build succeeds to verify Image component changes valid:
```bash
pnpm build
```
  </verify>
  <done>
app/(admin)/admin/landing-page/page.tsx uses .webp paths for all avatar images. Priority prop added to exactly ONE LCP candidate image (or none if all below-fold). No .png references remain in TSX files for these three avatars. TypeScript compilation succeeds. Production build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate bundle baseline and document metrics</name>
  <files>
    .planning/phases/27-foundation-quick-wins/27-BASELINE.md
  </files>
  <action>
Generate bundle size report using the bundle analyzer installed in Plan 01:
```bash
ANALYZE=true pnpm build
```

This creates three HTML files in .next/analyze/: client.html, nodejs.html, edge.html per research.

Open and analyze the reports:
```bash
open .next/analyze/client.html  # Or manually open in browser
```

Record the following baseline metrics in .planning/phases/27-foundation-quick-wins/27-BASELINE.md:

**Bundle Sizes:**
- Total client bundle size (from client.html)
- Largest client bundles (top 5 chunks)
- Server bundle size (from nodejs.html)
- Edge bundle size (from edge.html)

**Current Performance (from Lighthouse or manual testing):**
- Subscribe page LCP (current: ~3.66s per research)
- New chat page LCP
- Subscribe page Performance score
- New chat page Performance score

**Image Sizes:**
- Original PNG sizes (alex, kim, collaborative)
- WebP sizes after conversion
- Size reduction percentages

Document date and environment (Next.js version, Node version) for future comparison.

This baseline establishes the "before" state for Phase 31 validation (MON-02 through MON-05).
  </action>
  <verify>
Verify baseline document exists:
```bash
cat .planning/phases/27-foundation-quick-wins/27-BASELINE.md
```

Check it contains:
- Bundle size metrics (client/server/edge)
- LCP measurements for key pages
- Image size comparisons (PNG vs WebP)
- Timestamp and environment details
  </verify>
  <done>
ANALYZE=true pnpm build completed successfully. Bundle analyzer generated three HTML reports. 27-BASELINE.md created with comprehensive metrics: bundle sizes, LCP times, image size reductions, and environment details. Developer has documented starting point for Phase 31 performance validation.
  </done>
</task>

</tasks>

<verification>
Comprehensive verification of image optimization:

1. WebP files exist and are 50-70% smaller than original PNGs
2. All avatar Image components use .webp paths
3. Priority prop added to exactly ONE LCP image (or none if appropriate)
4. No .png references remain for the three avatars in TSX files
5. Bundle baseline documented with current sizes and LCP times
6. Production build succeeds with new image paths
7. Original PNGs still exist for rollback if needed
</verification>

<success_criteria>
- [ ] sharp installed as devDependency
- [ ] scripts/convert-avatars-to-webp.ts exists and converts 3 files
- [ ] public/images/alex-avatar.webp exists
- [ ] public/images/kim-avatar.webp exists
- [ ] public/images/collaborative-avatar.webp exists
- [ ] WebP files are 50-70% smaller than original PNGs
- [ ] app/(admin)/admin/landing-page/page.tsx uses .webp paths
- [ ] Priority prop added to at most ONE image in landing page
- [ ] grep finds zero .png references for these three avatars in app/ tsx files
- [ ] ANALYZE=true pnpm build succeeds and generates reports
- [ ] .planning/phases/27-foundation-quick-wins/27-BASELINE.md exists with metrics
- [ ] Baseline document includes bundle sizes, LCP times, image reductions
- [ ] npx tsc --noEmit passes
- [ ] pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-quick-wins/27-02-SUMMARY.md`
</output>
