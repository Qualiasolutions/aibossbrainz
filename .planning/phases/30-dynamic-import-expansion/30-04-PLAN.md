---
phase: 30-dynamic-import-expansion
plan: 04
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - .next/diagnostics/analyze/post-phase-30.txt
  - .planning/phases/30-dynamic-import-expansion/bundle-comparison.md
autonomous: false

must_haves:
  truths:
    - "Bundle size reduced by 30-40% on new chat page (target: 500KB → 300KB)"
    - "Stripe payment flow completes successfully in production build"
    - "No hydration errors in production build"
    - "All dynamic imports load correctly in production"
    - "Developer has comparison report showing before/after bundle metrics"
  artifacts:
    - path: ".next/diagnostics/analyze/post-phase-30.txt"
      provides: "Post-optimization bundle analysis"
      min_lines: 10
    - path: ".planning/phases/30-dynamic-import-expansion/bundle-comparison.md"
      provides: "Before/after bundle size comparison with key metrics"
      min_lines: 20
  key_links:
    - from: "baseline-phase-30.txt"
      to: "post-phase-30.txt"
      via: "Bundle size delta calculation"
      pattern: "\\d+\\s*KB.*\\d+\\s*KB"
---

<objective>
Verify optimizations, test Stripe flow, and measure bundle improvements

Purpose: Ensure all dynamic imports work in production, Stripe payment flow unbroken, and bundle size reduction achieved
Output: Post-optimization bundle analysis, comparison report, Stripe flow verified, production build validated
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-dynamic-import-expansion/30-RESEARCH.md

Research findings:
- Stripe API routes must never be dynamically imported (stay server-side)
- Current subscribe page uses server-side checkout session creation (safe)
- Target: 30-40% bundle reduction (500KB → 300KB)
- Production build required to verify code-splitting works correctly
- Must test Stripe embedded checkout flow in production build
</context>

<tasks>

<task type="auto">
  <name>Generate post-optimization bundle analysis</name>
  <files>.next/diagnostics/analyze/post-phase-30.txt</files>
  <action>
Run bundle analyzer after all optimizations applied:

1. Build with analyzer enabled:
   ```bash
   ANALYZE=true pnpm build 2>&1 | tee .next/diagnostics/analyze/post-phase-30.txt
   ```

2. Wait for build to complete and report generation

3. Verify .next/diagnostics/analyze/post-phase-30.txt contains:
   - Overall bundle size (should be smaller than baseline)
   - Client bundle breakdown
   - Main route chunk sizes for /new and /chat/[id]
   - Separate chunks for dynamically imported components (Chat, admin components, etc.)

4. Check for any build warnings or errors related to dynamic imports

5. Verify bundle analyzer HTML report opens (or disable openAnalyzer: false to skip browser)
  </action>
  <verify>
File .next/diagnostics/analyze/post-phase-30.txt exists with bundle size data. Verify dynamically imported components appear as separate chunks (not in main bundle).
  </verify>
  <done>
Post-optimization bundle analysis saved with chunk breakdown showing code-split components
  </done>
</task>

<task type="auto">
  <name>Create bundle comparison report</name>
  <files>.planning/phases/30-dynamic-import-expansion/bundle-comparison.md</files>
  <action>
Compare baseline and post-optimization bundle metrics:

1. Read baseline metrics from .next/diagnostics/analyze/baseline-phase-30.txt

2. Read post-optimization metrics from .next/diagnostics/analyze/post-phase-30.txt

3. Create bundle-comparison.md with structure:
   ```markdown
   # Phase 30: Bundle Size Comparison

   **Date:** 2026-03-01
   **Goal:** 30-40% reduction on new chat page (target: 500KB → 300KB)

   ## Overall Bundle Size

   | Metric | Baseline | Post-Optimization | Delta | % Change |
   |--------|----------|-------------------|-------|----------|
   | Total Client Bundle | [X] KB | [Y] KB | [Z] KB | [%] |
   | Main Route (/new) | [X] KB | [Y] KB | [Z] KB | [%] |
   | Chat Route (/chat/[id]) | [X] KB | [Y] KB | [Z] KB | [%] |
   | Admin Analytics | [X] KB | [Y] KB | [Z] KB | [%] |
   | Subscribe Page | [X] KB | [Y] KB | [Z] KB | [%] |

   ## Code-Split Chunks (New)

   | Chunk | Size | Component |
   |-------|------|-----------|
   | chat-[hash].js | [X] KB | ChatWithErrorBoundary |
   | executive-breakdown-[hash].js | [X] KB | ExecutiveBreakdown |
   | revenue-filter-[hash].js | [X] KB | RevenueFilter |
   | stats-card-[hash].js | [X] KB | StatsCard |

   ## Font Optimization

   | Metric | Baseline | Post-Optimization | Delta |
   |--------|----------|-------------------|-------|
   | Geist Sans (all weights/subsets) | [X] KB | - | Removed |
   | Geist Sans (latin, 400/500/600) | - | [Y] KB | Added |
   | Geist Mono (all weights/subsets) | [X] KB | - | Removed |
   | Geist Mono (latin, 400/500) | - | [Y] KB | Added |
   | Total Font Size | [X] KB | [Y] KB | [Z] KB |

   ## Component Removals

   | Component | Baseline Size | Post-Optimization | Savings |
   |-----------|---------------|-------------------|---------|
   | Radix Select (subscribe page) | ~29.5 KB | 0 KB | ~29.5 KB |

   ## Success Criteria Evaluation

   - [x/☐] New chat page FCP bundle reduced by 30-40%: [ACTUAL %]
   - [x/☐] Admin analytics charts separated into lazy-loaded chunks
   - [x/☐] Subscribe page using native select (Radix Select removed)
   - [x/☐] Geist fonts subset to latin with key weights only
   - [x/☐] Total bundle size reduction: [ACTUAL KB]

   ## Findings

   [Summary of optimization impact, unexpected results, areas for future improvement]
   ```

4. Fill in actual metrics from build output

5. Calculate deltas and percentage changes

6. Mark success criteria as achieved or not achieved with actual metrics
  </action>
  <verify>
File .planning/phases/30-dynamic-import-expansion/bundle-comparison.md exists with:
- Complete before/after metrics
- Delta calculations
- Success criteria evaluation
- At least 3 identified code-split chunks
  </verify>
  <done>
Bundle comparison report created with baseline vs post-optimization metrics, delta calculations, and success criteria evaluation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Dynamic imports applied to Chat component, admin analytics, fonts optimized, subscribe page using native select. Production build created with bundle analysis showing code-split chunks.
  </what-built>

  <how-to-verify>
## 1. Production Build Verification

Start production build:
```bash
pnpm build
pnpm start
```

## 2. Chat Component Dynamic Loading

- Visit http://localhost:3000/new
- Open DevTools → Network tab → JS filter
- Verify you see a separate chunk file loading for chat component (e.g., `chat-[hash].js`)
- Verify chat interface loads correctly
- Type a message and send - verify chat works normally
- Navigate to an existing chat URL
- Verify chat component loads correctly with message history

## 3. Font Optimization

- Refresh http://localhost:3000/new with Network tab open
- Filter by "font"
- Verify only latin subset fonts load (check font URLs)
- Verify only key weights load (400, 500, 600 for Sans; 400, 500 for Mono)
- Confirm font rendering looks identical to before optimization

## 4. Subscribe Page Native Select

- Visit http://localhost:3000/subscribe
- Verify industry dropdown is a native select (NOT a styled Radix dropdown)
- Verify styling matches form field design (height ~44px, border, padding)
- Open dropdown, verify all 12 industries appear
- Select "Technology" from dropdown
- Verify selection updates correctly
- Complete the form and attempt to proceed to payment
- DO NOT complete payment - just verify the form submission initiates Stripe checkout

## 5. Admin Analytics Dynamic Loading

- Login as admin user
- Visit http://localhost:3000/admin/analytics
- Open DevTools → Network tab → JS filter
- Verify separate chunk files load for analytics components
- Verify all charts render correctly
- Test filter interactions

## 6. Stripe Payment Flow (CRITICAL)

**IMPORTANT: This is the most critical verification. Stripe flow must work in production.**

Option A - Test Mode (Recommended):
- Visit http://localhost:3000/subscribe?plan=monthly
- Fill out the form with test data
- Click subscribe button
- Verify redirect to Stripe checkout page happens
- Use Stripe test card: 4242 4242 4242 4242, any future date, any CVC
- Complete the payment flow
- Verify redirect back to app with payment=success query param
- Verify subscription activation works

Option B - Visual Inspection Only:
- Verify /api/stripe/checkout/route.ts was NOT modified (should be server-side only)
- Verify subscribe page checkout session creation uses server action
- Confirm no Stripe.js code in dynamically imported components

## 7. Hydration Check

- Open browser console on http://localhost:3000/new
- Look for any React hydration warnings:
  - "Text content does not match server-rendered HTML"
  - "Hydration failed"
  - "Expected server HTML to contain..."
- If any hydration warnings appear, note the component and message

## 8. Bundle Comparison

- Review .planning/phases/30-dynamic-import-expansion/bundle-comparison.md
- Verify bundle size reduction achieved (target: 30-40% on /new route)
- Confirm code-split chunks listed
- Check if success criteria met

## Acceptance Criteria

Mark each as ✓ or ✗:

- [ ] Chat component loads as separate chunk in Network tab
- [ ] Chat functionality works identically (send/receive messages, history)
- [ ] Fonts load only latin subset with specified weights
- [ ] Font rendering looks correct (no layout shift, proper weights)
- [ ] Subscribe page industry dropdown is native select (not Radix)
- [ ] Native select styling matches form fields
- [ ] All 12 industries selectable
- [ ] Admin analytics charts load as separate chunks
- [ ] Analytics functionality works (charts render, filters work)
- [ ] Stripe checkout flow completes successfully (redirect works, payment processes)
- [ ] No hydration errors in browser console
- [ ] Bundle size reduced by 30-40% on /new route (check bundle-comparison.md)
- [ ] Production build completes with no errors

## If Issues Found

Describe any problems:
- What failed? (be specific)
- What error appeared? (exact message)
- Which component/page?
- Steps to reproduce

If all checks pass, respond: **"approved"**

If issues found, describe them and suggest fixes before proceeding.
  </how-to-verify>

  <resume-signal>
Type "approved" if all verifications pass, or describe issues found for debugging
  </resume-signal>
</task>

</tasks>

<verification>
Post-checkpoint verification (after human approval):

1. **Bundle metrics documented:**
   - Baseline and post-optimization metrics captured
   - Delta calculations correct
   - Success criteria evaluated

2. **Production build stable:**
   - No build errors
   - No runtime errors
   - No hydration warnings

3. **Stripe flow verified:**
   - Payment checkout works
   - Redirect flow correct
   - No code-splitting breakage

4. **All dynamic imports working:**
   - Chat component loads
   - Admin components load
   - No console errors
</verification>

<success_criteria>
1. Post-optimization bundle analysis generated and saved
2. Bundle comparison report created with before/after metrics
3. New chat page bundle reduced by 30-40% (target: 500KB → 300KB)
4. Code-split chunks visible in bundle analyzer (Chat, admin components)
5. Stripe payment flow completes successfully in production build
6. No hydration errors in browser console
7. All dynamic imports load correctly
8. Font optimization verified (latin subset only, key weights)
9. Subscribe page native select functional
10. Human verification completed with approval
</success_criteria>

<output>
After completion, create `.planning/phases/30-dynamic-import-expansion/30-04-SUMMARY.md`
</output>
