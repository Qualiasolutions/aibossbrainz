---
phase: 32-voice-call-redesign
plan: 04
type: execute
wave: 3
depends_on: ["32-01", "32-02", "32-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Chat interface is text-only (no voice buttons)"
    - "Call button exists in sidebar"
    - "Call modal opens and allows executive selection"
    - "Voice call connects and responds to speech"
    - "Audio plays AI responses"
    - "Hang up ends call cleanly"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete voice call redesign works end-to-end: chat is text-only, call modal functional, voice loop operational.

Purpose: Ensure Phase 32 requirements are met before considering the phase complete.
Output: Verified working system with text-only chat and dedicated voice calls
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-voice-call-redesign/32-RESEARCH.md
@.planning/phases/32-voice-call-redesign/32-01-SUMMARY.md
@.planning/phases/32-voice-call-redesign/32-02-SUMMARY.md
@.planning/phases/32-voice-call-redesign/32-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 32 Voice Call Redesign complete with:
- Text-only chat interface (no voice playback buttons, no auto-speak)
- Dedicated Call button in sidebar
- Premium call modal with executive selection
- Real-time voice loop (SpeechRecognition → AI → TTS)
- Visual feedback (waveform visualizer, state indicators)
  </what-built>

  <how-to-verify>
**Prerequisites:**
- Browser: Chrome, Edge, or Safari (SpeechRecognition support required)
- Microphone: Grant permission when prompted
- Environment: Development server running (`pnpm dev`)

**Verification Steps:**

1. **Chat is text-only (no voice features):**
   - Open chat at http://localhost:3000
   - Start a new chat, send a text message
   - ✓ Verify: Message appears without voice playback button
   - ✓ Verify: AI response appears without auto-playing audio
   - ✓ Verify: Input area has no voice mode toggle button

2. **Sidebar has Call button:**
   - Look at the sidebar header area
   - ✓ Verify: "Call" button with phone icon exists
   - ✓ Verify: "Clear" button (Trash icon) is gone
   - ✓ Verify: Voice toggle button (AudioLines icon) is gone

3. **Call modal opens:**
   - Click the "Call" button in sidebar
   - ✓ Verify: Modal opens with "Who would you like to call?" heading
   - ✓ Verify: Two executive cards shown (Alexandria and Kim)
   - ✓ Verify: Each card has name, tagline, and gradient styling

4. **Executive selection works:**
   - Click on Alexandria's card
   - ✓ Verify: Modal transitions to call interface (no longer showing selection)
   - ✓ Verify: Alexandria's name/avatar shown at top
   - ✓ Verify: Waveform visualizer visible in center
   - ✓ Verify: "Hang Up" button visible at bottom

5. **Voice call functionality (CRITICAL TEST):**
   - Browser prompts for microphone permission → Allow
   - Wait for "Listening..." state indicator
   - ✓ Verify: Waveform bars animate
   - Speak into microphone: "Hello Alexandria, can you help me with brand strategy?"
   - ✓ Verify: State changes to "Thinking..."
   - ✓ Verify: State changes to "Speaking..." when AI responds
   - ✓ Verify: Audio plays (hear Alexandria's voice)
   - ✓ Verify: After audio ends, state returns to "Listening..."
   - Speak again: "What's the first step?"
   - ✓ Verify: Voice loop continues (thinking → speaking → listening cycle)

6. **Hang up works:**
   - Click "Hang Up" button
   - ✓ Verify: Audio stops immediately if playing
   - ✓ Verify: Modal closes
   - ✓ Verify: Microphone stops listening (browser mic indicator off)
   - ✓ Verify: Can open modal again and start new call

7. **Kim executive works:**
   - Open Call modal again
   - Select Kim this time
   - Speak: "Kim, can you help with sales strategy?"
   - ✓ Verify: Kim's voice responds (different voice from Alexandria)
   - Hang up

8. **Modal dismissal prevented during call:**
   - Open Call modal, select Alexandria
   - Wait for call to start (Listening state)
   - Try pressing Escape key
   - ✓ Verify: Modal does NOT close (call stays active)
   - Try clicking outside modal
   - ✓ Verify: Modal does NOT close (call stays active)
   - Click Hang Up
   - ✓ Verify: Modal closes

9. **Build verification:**
   ```bash
   npm run build
   ```
   - ✓ Verify: Build succeeds with no errors
   - ✓ Verify: No warnings about missing voice components

10. **TypeScript verification:**
    ```bash
    npx tsc --noEmit
    ```
    - ✓ Verify: No type errors reported

**Known limitations (OK for v1):**
- Voice calls are ephemeral (no transcript saved to chat history)
- Waveform is animated bars (not real audio frequency analysis)
- No mute/volume controls in call UI
- Firefox not supported (no SpeechRecognition API)

**Common issues to check:**
- If microphone doesn't work: Check browser permissions
- If audio doesn't play: Check /api/realtime/stream returns audioUrl or audioData
- If call state gets stuck: Check browser console for errors, verify /api/realtime/stream response
- If modal won't close after hangup: Check onClose handler in app-sidebar.tsx
  </how-to-verify>

  <resume-signal>
Type "approved" if all checks pass, or describe specific issues found
  </resume-signal>
</task>

</tasks>

<verification>
All verification steps performed in checkpoint task above.
</verification>

<success_criteria>
- [ ] Chat is text-only with no voice buttons
- [ ] Sidebar has Call button (Clear and Voice buttons removed)
- [ ] Call modal opens with executive selection
- [ ] Voice call connects and responds to speech
- [ ] Audio plays AI responses via ElevenLabs
- [ ] Hang up ends call cleanly
- [ ] Modal dismissal prevented during active call
- [ ] Build passes with no errors
- [ ] TypeScript passes with no errors
</success_criteria>

<output>
After user approval, create `.planning/phases/32-voice-call-redesign/32-04-SUMMARY.md` documenting:
- What was verified
- Any issues found and resolved
- Performance observations (voice latency, audio quality)
- User feedback if provided
- Phase 32 completion status
</output>
