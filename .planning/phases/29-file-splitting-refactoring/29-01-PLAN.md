---
phase: 29-file-splitting-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/icons/navigation.tsx
  - components/icons/actions.tsx
  - components/icons/status.tsx
  - components/icons/brand.tsx
  - components/icons/content.tsx
  - components/icons/misc.tsx
autonomous: true

must_haves:
  truths:
    - "Icons are organized into semantic category files (navigation, actions, status, brand, content, misc)"
    - "Each category file exports icons directly without barrel re-exports"
    - "TypeScript compilation succeeds with zero errors after split"
  artifacts:
    - path: "components/icons/navigation.tsx"
      provides: "Navigation-related icons (Home, Menu, Chevron, etc.)"
      exports: ["HomeIcon", "MenuIcon", "SidebarLeftIcon", "ChevronDownIcon", "ChevronUpIcon", "ChevronLeftIcon", "ChevronRightIcon"]
      min_lines: 100
    - path: "components/icons/actions.tsx"
      provides: "Action icons (Copy, Trash, Upload, Plus, Cross, etc.)"
      exports: ["CopyIcon", "TrashIcon", "UploadIcon", "PlusIcon", "CrossIcon", "SendHorizontalIcon", "EditIcon", "DownloadIcon", "RefreshIcon", "SettingsIcon"]
      min_lines: 150
    - path: "components/icons/status.tsx"
      provides: "Status and feedback icons (Loader, Check, Thumbs, etc.)"
      exports: ["LoaderIcon", "CheckCircleFillIcon", "ThumbUpIcon", "ThumbDownIcon", "AlertCircleIcon", "InfoIcon"]
      min_lines: 80
    - path: "components/icons/brand.tsx"
      provides: "Brand and service logos (OpenAI, Google, Anthropic, Vercel)"
      exports: ["LogoOpenAI", "LogoGoogle", "LogoAnthropic", "VercelIcon"]
      min_lines: 60
    - path: "components/icons/content.tsx"
      provides: "Content type icons (File, Image, Message, Code, Terminal)"
      exports: ["FileIcon", "ImageIcon", "MessageIcon", "CodeIcon", "TerminalIcon", "DocumentIcon", "SparklesIcon"]
      min_lines: 100
    - path: "components/icons/misc.tsx"
      provides: "Miscellaneous icons (Bot, User, Attachment, etc.)"
      exports: ["BotIcon", "UserIcon", "AttachmentIcon", "ArrowIcon"]
      min_lines: 50
  key_links:
    - from: "components/icons/actions.tsx"
      to: "components/icons.tsx"
      via: "extracted from original"
      pattern: "export const .* = .*IconProps.*"
    - from: "all category files"
      to: "TypeScript compilation"
      via: "import type { SVGProps }"
      pattern: "type IconProps = SVGProps"
---

<objective>
Split icons.tsx (1274 lines, 54 icons) into 6 semantic category files to enable tree-shaking and reduce bundle size.

Purpose: Enable Next.js bundler to tree-shake unused icons, reducing client bundle size by 5-15%. Current monolithic file forces all 54 icons to be included in any route that imports even one icon.

Output: 6 category-based icon files with direct exports (no barrel index.ts)
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-file-splitting-refactoring/29-RESEARCH.md

# Source file
@components/icons.tsx
</context>

<tasks>

<task type="auto">
  <name>Create icon category directory structure</name>
  <files>
    components/icons/navigation.tsx
    components/icons/actions.tsx
    components/icons/status.tsx
    components/icons/brand.tsx
    components/icons/content.tsx
    components/icons/misc.tsx
  </files>
  <action>
Create `components/icons/` directory and split icons.tsx into 6 category files based on semantic grouping:

**navigation.tsx** (navigation/UI chrome):
- HomeIcon, MenuIcon, SidebarLeftIcon, SidebarRightIcon
- ChevronDownIcon, ChevronUpIcon, ChevronLeftIcon, ChevronRightIcon
- ArrowUpIcon, ArrowDownIcon, ArrowLeftIcon, ArrowRightIcon

**actions.tsx** (user actions):
- CopyIcon, TrashIcon, UploadIcon, DownloadIcon
- PlusIcon, CrossIcon, EditIcon, RefreshIcon
- SendHorizontalIcon, SettingsIcon, SearchIcon

**status.tsx** (status indicators/feedback):
- LoaderIcon, CheckCircleFillIcon, CheckIcon
- ThumbUpIcon, ThumbDownIcon
- AlertCircleIcon, InfoIcon, WarningIcon

**brand.tsx** (brand logos):
- LogoOpenAI, LogoGoogle, LogoAnthropic
- VercelIcon, GitHubIcon

**content.tsx** (content types):
- FileIcon, ImageIcon, MessageIcon
- CodeIcon, TerminalIcon, DocumentIcon
- SparklesIcon, TextIcon

**misc.tsx** (uncategorized):
- BotIcon, UserIcon, AttachmentIcon
- Any remaining icons not fitting above categories

Each file structure:
```typescript
import type { SVGProps } from "react";

type IconProps = SVGProps<SVGSVGElement> & { size?: number };

export const IconName = ({ size = 16, ...props }: IconProps) => (
  <svg height={size} width={size} viewBox="0 0 16 16" style={{ color: "currentcolor" }} {...props}>
    {/* SVG path from original */}
  </svg>
);
```

CRITICAL: DO NOT create index.ts barrel file - kills tree-shaking per research (barrel exports prevent bundler from eliminating unused icons).

Reference icons.tsx for exact SVG paths and preserve all attributes.
  </action>
  <verify>
```bash
# Verify all 6 category files exist
ls components/icons/*.tsx | wc -l  # Should be 6

# Verify no barrel export file
! test -f components/icons/index.ts

# Count total exported icons across all files
grep "^export const.*Icon = " components/icons/*.tsx | wc -l  # Should be 54

# TypeScript compilation
npx tsc --noEmit
```
  </verify>
  <done>
- 6 category files exist in components/icons/
- Each file exports icons using direct named exports
- No index.ts barrel file exists
- All 54 icons accounted for across category files
- TypeScript compilation succeeds with zero errors
  </done>
</task>

</tasks>

<verification>
1. All 54 icons from original icons.tsx are present in category files
2. No barrel export (index.ts) exists
3. TypeScript compilation passes (`npx tsc --noEmit`)
4. Each category file has consistent IconProps type
5. SVG paths preserved exactly from original
</verification>

<success_criteria>
- 6 category files created with semantic grouping
- Total icon count matches original (54 icons)
- No barrel exports created
- TypeScript compiles without errors
- Ready for consumer migration in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/29-file-splitting-refactoring/29-01-SUMMARY.md`
</output>
